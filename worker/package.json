{
  "name": "signal-bot-worker",
  "version": "0.1.0",
  "private": true,
  "description": "SignalBot на Cloudflare Workers: Telegram webhook + KV + cron",
  "scripts": {
    "types": "wrangler types ./src/env.d.ts",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "./scripts/deploy.sh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.4.0",
    "wrangler": "^4.131.1"
  }
}